name : New Pull Request Created

on:
  pull_request:
    types: [opened]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Get PR Reviewers
        id: get_reviewers
        run: |
          reviewers=$(curl -s -L \
            -H "Authorization: Bearer ${{ secrets.PROJECT_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            -X GET "https://api.github.com/repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/requested_reviewers")

          echo $(reviewers | jq -r '.users[].login')
          # echo "reviewers=$(reviewers | jq -r '.users[].login')" >> $GITHUB_OUTPUT
        env:
          GITHUB_TOKEN: ${{ secrets.PROJECT_TOKEN }}
      - name: test-log2
        run: |
          reviewers=$(curl -s -L \
            -H "Authorization: Bearer ${{ secrets.PROJECT_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            -X GET "https://api.github.com/repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/requested_reviewers")

          echo $reviewers | jq -r '.users[].login'
        env:
          GITHUB_TOKEN: ${{ secrets.PROJECT_TOKEN }}
      - name: env-test
        id: test1
        run: |
          echo "test=Hello world" >> $GITHUB_OUTPUT
      - name: test-log1
        run: |
          echo "${{ steps.test1.outputs.test }}"
      - name: test-log
        run: |
          echo "${{ steps.get_reviewers.outputs.reviewers }}"
      - name: action-slack
        uses: 8398a7/action-slack@v3
        with:
          status: custom
          icon_emoji: ':octocat:'
          custom_payload: |
            {
              text: "Pull Request Opened!",
              attachments: [{
                author_name: "Author : ${{ github.event.pull_request.user.login }}",
                fallback: 'fallback',
                color: 'good',
                title: 'PR Reviewers',
                text: '$reviewers_list',
                fields: [{
                  title: 'PR Title',
                  value: '${{ github.event.pull_request.title }}',
                  short: true
                },
                {
                  title: 'reverse',
                  value: 'gnirts esrever'.split('').reverse().join(''),
                  short: true
                },
                {
                  title: 'long title1',
                  value: 'long value1',
                  short: false
                }],
                actions: [{
                }]
              }]
            }
          if_mention: failure,cancelled
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        if: always()
